{{extend 'layout2.html'}}
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Reviews</title>
        <style type="text/css">
  body {
  padding-left: 11em;
  font-family: Georgia, "Times New Roman",
        Times, serif;
   }
iframe
{
float:right;
}
ul.navbar {
  list-style-type: none;
  padding: 0;
  margin: 0;
float: left;
  position: absolute;
  top: 11em;
  left: 1em;
  width: 8em }
ul.navbar li {
  background: grey;
  margin: 0.5em 0;
  padding: 0.3em;
  border-right: thin dotted }
ul.navbar a {
color: blue;
  text-decoration: none }
a:link {
  color: red }
a:visited {
  color: red }

  </style>
</head>
<body>


<iframe id="iframe" width="1100" height="500" src="{{=URL('SpiceJet')}}" name="{{=URL('SpiceJet')}}"></iframe>

<!-- Site navigation menu -->
<ul class="navbar">
  <li><a href="{{=URL('SpiceJet')}}" target="{{=URL('SpiceJet')}}" >Spice Jet</a>
  <li><a href="{{=URL('AirIndia')}}" target="{{=URL('SpiceJet')}}">Air India</a>
  <li><a href="{{=URL('Indigo')}}" target="{{=URL('SpiceJet')}}">Indigo</a>
    </li>
    </ul>
<center><a href="{{=URL('index')}}">Back</button></a></center>

</body>
    


</html>
